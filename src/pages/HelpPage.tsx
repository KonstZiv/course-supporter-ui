// Є3 — the address the header help seam (Є1) points at. Р10 keeps step Є to the
// seam alone: no help content is written yet, so the page is a single honest
// sentence. Future content lands here without touching the header seam again.
export function HelpPage() {
  return (
    <div className="max-w-[1600px] mx-auto px-6 py-8">
      <h1 className="page-title">Довідка</h1>
      <p className="page-subtitle">Цей розділ ще готується.</p>
    </div>
  )
}
