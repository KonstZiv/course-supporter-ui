// Loaded by vitest (see vitest.config.ts setupFiles). Imports
// jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.) and
// extends Vitest's `expect`. Future global test hooks (e.g. cleanup,
// MSW server lifecycle) belong here too.
import '@testing-library/jest-dom'
